public int maximizeSum(int[] nums, int k) {
        Arrays.sort(nums);
        int max =  nums[nums.length -1];
        for(int i =1; i<k;i++){
          max +=  nums[nums.length -1]+i;
        }
        return max;
    }
